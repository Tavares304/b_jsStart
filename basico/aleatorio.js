let resultado = Math.random();

//console.log("Resultado: " + resultado);
console.log(`Resultado: ${resultado}`);


if (resultado >= 0.5) {
    console.log("Sua resposta é SIM.");
} else {
    console.log("Sua resposta é NÃO.");
}

// poderia ser feito assim: let resposta = Math.random() >= 0.5 ? “SIM” : “NÃO”;

